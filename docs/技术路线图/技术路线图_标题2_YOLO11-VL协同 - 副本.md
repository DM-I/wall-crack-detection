# 技术路线图：YOLO11-VL — 一种视觉检测与多模态分析协同的建筑外墙裂缝智能诊断方法

```mermaid
flowchart TD
    subgraph 数据采集层["多源影像数据采集"]
        A1["🎥 视频流影像<br/>无人机/手持巡检录像"]
        A2["📷 地面移动端影像<br/>智能手机/相机近景拍摄"]
        A3["🛸 无人机航拍影像<br/>网格化航线立面拍摄"]
    end

    subgraph 预处理层["统一预处理"]
        B1["视频抽帧<br/>间隔30帧提取关键帧"]
        B2["格式标准化<br/>PNG/JPG/BMP/TIFF"]
        B3["尺寸统一<br/>640×640像素"]
        B4["EXIF信息提取<br/>记录拍摄参数与位置"]
    end

    A1 --> B1 --> B2
    A2 --> B2
    A3 --> B2
    B2 --> B3 --> B4

    subgraph 视觉检测层["YOLO11 视觉检测分支"]
        C1["CSPNet骨干网络<br/>多层次特征提取"]
        C2["PAN-FPN颈部<br/>多尺度特征融合"]
        C3["解耦检测头<br/>分类 + 边界框回归"]
        C4["裂缝检测输出<br/>• 裂缝位置与边界框<br/>• 裂缝类型分类<br/>• 置信度评分"]
    end

    B4 --> C1 --> C2 --> C3 --> C4

    subgraph 多模态分析层["QwenVL 多模态分析分支"]
        D1["视觉-语言对齐<br/>检测框叠加原图"]
        D2["上下文理解<br/>裂缝形态 + 建筑背景"]
        D3["专业分析推理<br/>• 裂缝严重程度评估<br/>• 成因机制分析<br/>• 扩展趋势预判"]
        D4["修复建议生成<br/>• 修复方案推荐<br/>• 施工优先级排序<br/>• 材料工艺建议"]
    end

    C4 -->|"检测结果 + 原图"| D1 --> D2 --> D3 --> D4

    subgraph 协同融合层["双模型协同融合"]
        E1["YOLO输出<br/>定量检测数据<br/>坐标·类别·置信度"]
        E2["QwenVL输出<br/>定性分析文本<br/>评估·归因·建议"]
        E3["特征对齐与互补<br/>定量 + 定性 = 完整诊断"]
    end

    C4 --> E1
    D4 --> E2
    E1 --> E3
    E2 --> E3

    subgraph 输出层["智能诊断输出"]
        F1["结构化审核报告"]
        F2["裂缝分布可视化"]
        F3["风险评估等级"]
        F4["修复方案文档"]
    end

    E3 --> F1
    E3 --> F2
    E3 --> F3
    E3 --> F4

    style 视觉检测层 fill:#e3f2fd,stroke:#1565c0
    style 多模态分析层 fill:#fce4ec,stroke:#c62828
    style 协同融合层 fill:#f3e5f5,stroke:#7b1fa2
    style 输出层 fill:#e8f5e9,stroke:#2e7d32
```

> **路线图说明**：本技术路线以 **YOLO11 视觉检测**与 **QwenVL 多模态分析**的协同为核心，体现"定量检测 + 定性分析 → 完整诊断"的创新范式。两条分支既独立运行又互相增强——YOLO11 提供精确的裂缝定位与分类数据，QwenVL 在此基础上进行专业级语义理解与推理，最终融合生成结构化诊断报告。
