# 技术路线图：低空遥感驱动的建筑外墙裂缝智能检测——融合YOLO11与视觉语言模型的全流程方法

```mermaid
flowchart LR
    subgraph L1["一、低空遥感数据采集"]
        direction TB
        A1["无人机平台搭载<br/>高分辨率可见光相机"]
        A2["网格化航线规划<br/>建筑立面分区 / 重叠率≥60%"]
        A3["空地协同多源采集<br/>无人机普查 ⊗ 地面特写 ⊗ 视频流"]
        A1 --> A2 --> A3
    end

    subgraph L2["二、数据预处理与标注"]
        direction TB
        B1["影像预处理<br/>去畸变 · 去雾增强 · EXIF提取"]
        B2["裂缝标注体系<br/>边界框标注 · 类型分类 · 严重程度分级"]
        B3["数据集构建<br/>训练集/验证集/测试集 · 增强与平衡"]
        B1 --> B2 --> B3
    end

    subgraph L3["三、YOLO11模型训练"]
        direction TB
        C1["模型选型<br/>YOLO11n / s / m 多规格可选"]
        C2["训练策略<br/>SGD+余弦退火 · Mosaic增强 · 早停"]
        C3["损失函数<br/>CIoU边界框回归 + BCE分类 + DFL"]
        C4["评估指标<br/>mAP50 · mAP50-95 · P/R · FPS"]
        C1 --> C2 --> C3 --> C4
        C4 -.->|"迭代优化"| C2
    end

    subgraph L4["四、多源影像裂缝检测"]
        direction TB
        D1["多源影像输入<br/>航拍图 / 地面图 / 视频帧"]
        D2["YOLO11推理引擎<br/>裂缝定位 · 类型分类 · 置信度评分"]
        D3["后处理<br/>NMS去重 · SAHI小目标增强"]
        D1 --> D2 --> D3
    end

    subgraph L5["五、QwenVL智能分析"]
        direction TB
        E1["多模态输入<br/>检测框叠加原图 + 分析提示"]
        E2["语义理解<br/>裂缝形态 · 严重程度 · 成因归因"]
        E3["决策输出<br/>修复工艺推荐 · 优先级排序"]
        E1 --> E2 --> E3
    end

    subgraph L6["六、报告生成与平台"]
        direction TB
        F1["可视化<br/>检测框叠加 · 热力图 · 统计分析"]
        F2["审核报告<br/>概况统计 · 逐缝详情 · 修复建议"]
        F3["Web平台<br/>数据管理 · 训练监控 · 历史回溯"]
        F1 --> F2 --> F3
    end

    L1 --> L2 --> L3 --> L4
    L4 --> L5
    L4 --> F1
    L5 --> F2
    L5 -.->|"语义反馈<br/>降低误报"| L4

    style L1 fill:#e1f5fe,stroke:#0277bd,stroke-width:2px
    style L2 fill:#fff3e0,stroke:#e65100,stroke-width:2px
    style L3 fill:#e3f2fd,stroke:#1565c0,stroke-width:2px
    style L4 fill:#e8f5e9,stroke:#2e7d32,stroke-width:2px
    style L5 fill:#fce4ec,stroke:#c62828,stroke-width:2px
    style L6 fill:#ede7f6,stroke:#4527a0,stroke-width:2px
```

> **路线图说明**：以**低空遥感（无人机）**为数据驱动起点，构建"采集→标注→训练→检测→分析→报告"六阶段全流程闭环。无人机作为核心遥感平台完成大面积快速普查，地面端与视频流提供细节补充，YOLO11承担高效视觉检测，QwenVL完成专业语义分析，最终通过Web平台实现从原始影像到结构化诊断报告的全链路自动化。
